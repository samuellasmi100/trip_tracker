import React, { useState } from "react";
import CreateFamilyView from "./CreateFamily.view";

const CreateFamily = (props) => {
  return <CreateFamilyView {...props} />;
};

export default CreateFamily;
